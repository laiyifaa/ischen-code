package leetcode.array;

public class MlongestMountain {

    //845. 数组中的最长山脉
    public int longestMountain(int[] arr) {
        if (null == arr || arr.length < 3)
            return 0;
        return 0;
    }
}
